class Compute
{
    String average(int A[], int N)
    {
       double sum =0 ;
       double average = 0;
       for(int i =0 ; i<N ; i++){ 
          sum = sum + A[i];
          average = (sum/N);
          
           
       }
       return String.format("%.2f" , average);
    }
}
